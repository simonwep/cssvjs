const sideOrCorner = require('./sideOrCorner');
const percentage = require('./percentage');
const position = require('./position');
const length = require('./length');
const angle = require('./angle');

const types = {
    sideOrCorner,
    percentage,
    position,
    length,
    angle
};

module.exports = types;
